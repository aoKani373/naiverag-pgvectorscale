[project]
name = "naiverag-pgvectorscale"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.127.0",
    "python-dotenv>=1.2.1",
    "sqlmodel>=0.0.29",
]
